<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash Screen Theme (pre Android 12) -->
    <style name="Theme.App.Starting" parent="Theme.AppCompat">
        <item name="windowSplashScreenBackground">@color/teal_700</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/purple_700</item>
        <item name="postSplashScreenTheme">@style/Theme.Meloday20</item>
    </style>

</resources>